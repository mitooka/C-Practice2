#include <stdio.h>
int main(void){
    int nx;
    double dx;
    nx = 9.99;
    dx = 9.99;
    printf("int型変数nxの値は%dで、nx/2の値は%dです。\n",nx,nx/2);
    printf("double型変数dxの値は%fで、dx?2.0の値は%fです。\n",dx,dx/2);
    return(0);
    }